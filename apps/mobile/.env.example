# Backend API base URL. Defaults to the production API if unset — override for local
# development against a local backend (e.g. http://127.0.0.1:8000/api). Must be prefixed
# with EXPO_PUBLIC_ to be readable by the Expo app at runtime.
EXPO_PUBLIC_API_URL=
